class BlackJack:

  def __init__(self, user): #this creates the whole thing
        #get game
            #get's active game for user or creates a new one
        #get player
            #get's active player for user or creates a new one
        #get dealer
            #get's active dealer for user or creates a new one
        #get hand(s)                    

  #methods to create:
      #__init__(user) pass the user, grab the active game, active player, dealer, active hand(s), save everything as instance variables of the game
      #double_down
      #split
      #bet
      #stay
      #hit
